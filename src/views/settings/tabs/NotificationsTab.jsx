import settingsStyles from "../../SettingsView.module.css";

export default function Notifications() {
    return (
        <div>
            <h1 className={settingsStyles.tabTitle}>Notifications</h1>
        </div>
    );
};